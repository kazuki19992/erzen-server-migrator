{
  "name": "modpack-migrator",
  "version": "0.1.0",
  "private": true,
  "description": "Minecraft modpack migration helper (options.txt + journeymap)",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && node scripts/copy-static.mjs",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "electron": "^30.0.0",
    "typescript": "^5.4.0"
  }
}
